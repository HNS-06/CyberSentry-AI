import 'package:flutter/widgets.dart';

class HoneypotMatrix extends StatelessWidget {
  const HoneypotMatrix({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
