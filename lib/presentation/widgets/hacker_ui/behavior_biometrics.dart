import 'package:flutter/widgets.dart';

class BehaviorBiometrics extends StatelessWidget {
  const BehaviorBiometrics({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
