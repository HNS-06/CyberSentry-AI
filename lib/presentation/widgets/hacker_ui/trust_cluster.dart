import 'package:flutter/widgets.dart';

class TrustCluster extends StatelessWidget {
  const TrustCluster({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
