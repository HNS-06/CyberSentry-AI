import 'package:flutter/widgets.dart';

class QuantumVault extends StatelessWidget {
  const QuantumVault({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
